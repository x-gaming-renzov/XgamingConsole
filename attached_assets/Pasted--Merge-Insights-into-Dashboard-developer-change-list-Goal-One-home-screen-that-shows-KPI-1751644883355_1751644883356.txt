### Merge “Insights” into “Dashboard” — developer change list

*Goal: One home screen that shows KPIs **and** guidance; no separate Insights route.*

---

## 1 Navigation & route cleanup

| Action                                                                       | File(s)                  |
| ---------------------------------------------------------------------------- | ------------------------ |
| Remove **Insights** item from sidebar nav list                               | `Sidebar.tsx`, route map |
| Delete `/insights` route; add `redirect /insights → /dashboard` to avoid 404 | `AppRoutes.tsx`          |
| Delete “Insights” quick-action if present                                    | `QuickActions.tsx`       |

---

## 2 Dashboard layout update (wireframe)

```
┌───────────────── top bar ────────────────────────────────┐
│  metric cards (4)                                        │
└───────────────────────────────────────────────────────────┘

┌──────────── row A ────────────────┬──────── row A ───────┐
│  Top-Performing Experiences  (TPE)│ Campaign Health (CH) │
│  height 240 px                    │ height 240 px        │
└───────────────────────────────────┴───────────────────────┘

┌──────────────────── row B ───────────────────────────────┐
│  Optimization Ideas   (OI)   height 260 px               │
└───────────────────────────────────────────────────────────┘

┌──────────────────── row C ───────────────────────────────┐
│  Active Campaigns carousel (existing)                    │
└───────────────────────────────────────────────────────────┘
```

*If viewport < 1280 px stack CH under TPE; OI follows.*

---

## 3 Component moves & props

| From screen | Component                                   | Destination / placement                                                               | Notes                                    |
| ----------- | ------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------- |
| Insights    | `<TopExperiencesCard>`                      | row A left                                                                            | Accepts `limit=5` (prop)                 |
| Insights    | `<CampaignHealthCard>`                      | row A right                                                                           | Accepts `sparkline=false` on L screens   |
| Insights    | `<OptimizationIdeas>`                       | row B full-width                                                                      | Keeps internal ideas list/AI placeholder |
| Insights    | Footer stats (Avg uplift, Winners, Players) | **Convert to extra metric cards?**—Decision: **No** (dup w/ top row) – remove for now |                                          |

Remove duplicate fetches; reuse `/metrics/insights` endpoint.

---

## 4 API calls & state

1. **dashboardSlice.ts**

   ```ts
   fetchDashboard() {
     await Promise.all([
       api.get('/metrics/overview'),       // existing
       api.get('/metrics/top_experiences'),// for TPE
       api.get('/metrics/campaign_health'),// for CH
       api.get('/metrics/ideas'),          // for OI
     ]);
   }
   ```
2. Memoize selectors so Home renders once.

---

## 5 Copy & tooltips (unchanged)

* Card headlines remain identical.
* No “No data” text changes needed.

---

## 6 Responsive tweaks

*Breakpoints in `Dashboard.module.css`*

```css
@media (max-width: 1279px) {
  .rowA { flex-direction: column; gap: var(--spacing-lg); }
}
```

---

## 7 Clean-up purge

* Delete `InsightsPage.tsx`, route export.
* Remove unused images/icons imported only by Insights.
* Run `tsc --noEmit` & ESLint to ensure no missing imports.

---

## 8 QA checklist

* [ ] Sidebar has **Dashboard, Experiences, Objects, Campaigns** only.
* [ ] Visiting `/insights` redirects to `/dashboard`.
* [ ] Dashboard shows TPE & CH cards with mock data when no experiences yet (“No performance data available”).
* [ ] Scroll performance still < 60 fps (check heavy charts).
* [ ] Mobile view stacks cards in correct order.
* [ ] Unit tests updated (`Dashboard.test.tsx` snapshot includes new cards).

---

### Roll-out plan

1. **Branch** `feature/dashboard-insights-merge`.
2. Implement component moves; feature-flag behind `insightsMerge=true`.
3. QA on staging; remove flag & release. Total effort ≈ **5–6 dev hrs** (mostly JSX moves + CSS).

Once merged, PMs land on a single, richer home that answers **“How are we doing and what should I do next?”** without extra navigation.
